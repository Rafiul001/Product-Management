# Base URL of the API server (NO trailing slash).
# Local dev:
VITE_API_HOST_NAME=http://localhost:3000
# Production (set this in the Netlify dashboard, not here):
#   VITE_API_HOST_NAME=https://your-api.onrender.com
